# Rate Limiting
slowapi>=0.1.0,<1.0.0  # FastAPI rate limiting middleware
gunicorn>=21.0.0,<23.0.0
# [AI GENERATED]
# Model: GitHub Copilot (GPT-4.1)
# Logic: Production dependencies for Alfred backend (web, DB, LLM, utils, security, observability, background jobs).
# Why: Ensures all required packages are installed for backend to run and scale.
# Root Cause: Missing or outdated dependencies cause runtime errors and security issues.
# Context: Used by pip install in CI and prod. Future: review for security and pin versions.
# Model Suitability: Requirements logic is standard; GPT-4.1 is sufficient.
# Alfred Production Dependencies

# Web Framework
fastapi>=0.100.0,<1.0.0
uvicorn[standard]>=0.23.0,<1.0.0
gunicorn>=21.0.0,<23.0.0

# Database
sqlmodel>=0.0.14,<1.0.0
psycopg2-binary>=2.9.0,<3.0.0  # PostgreSQL adapter
aiosqlite>=0.19.0,<1.0.0       # Async SQLite support
alembic>=1.12.0,<2.0.0         # Database migrations

# LLM Integration
litellm>=1.0.0,<2.0.0


# Utilities
python-dotenv>=1.0.0,<2.0.0
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
redis[async]>=4.5.0,<6.0.0  # Redis with asyncio support for caching, queues

# Security
python-multipart>=0.0.6,<1.0.0

# HTTP Client (for notifications)
httpx>=0.25.0,<1.0.0

# Resilience
tenacity>=8.2.0,<9.0.0  # Retry logic with exponential backoff

# Observability
structlog>=23.0.0,<25.0.0      # Structured logging (optional enhancement)
prometheus-fastapi-instrumentator>=6.0.0,<7.0.0
# Redis + RQ for background workers
redis>=4.5.0,<5.0.0
rq>=1.13.0,<2.0.0
