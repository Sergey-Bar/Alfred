{{- /*
Alfred Helm Chart Installation Notes
Displayed after successful installation
*/ -}}

███████╗██╗     ███████╗██████╗ ███████╗██████╗ 
██╔════╝██║     ██╔════╝██╔══██╗██╔════╝██╔══██╗
███████╗██║     █████╗  ██████╔╝█████╗  ██║  ██║
██╔════╝██║     ██╔══╝  ██╔══██╗██╔══╝  ██║  ██║
███████╗███████╗██║     ██║  ██║███████╗██████╔╝
╚══════╝╚══════╝╚═╝     ╚═╝  ╚═╝╚══════╝╚═════╝ 

Enterprise AI Credit Governance Platform

Chart Version: {{ .Chart.Version }}
App Version: {{ .Chart.AppVersion }}
Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Alfred has been successfully deployed!

🔗 Access Instructions:

{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  🌐 Application URL: https://{{ .host }}
{{- end }}
{{- else }}
  To access Alfred, run these commands:

  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "alfred.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8000:8000
  
  Then visit: http://localhost:8000
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 Deployment Status:

  Replicas: {{ .Values.replicaCount }}
  {{- if .Values.autoscaling.enabled }}
  Autoscaling: Enabled ({{ .Values.autoscaling.minReplicas }}-{{ .Values.autoscaling.maxReplicas }} replicas)
  {{- end }}
  Redis: {{ if .Values.redis.enabled }}✅ Enabled{{ else }}❌ Disabled{{ end }}
  Workers: {{ if .Values.worker.enabled }}✅ Enabled ({{ .Values.worker.replicaCount }} replicas){{ else }}❌ Disabled{{ end }}
  Monitoring: {{ if .Values.monitoring.enabled }}✅ Enabled{{ else }}❌ Disabled{{ end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🩺 Health Check:

  kubectl --namespace {{ .Release.Namespace }} get pods -l "app.kubernetes.io/instance={{ .Release.Name }}"
  
  kubectl --namespace {{ .Release.Namespace }} logs -l "app.kubernetes.io/name={{ include "alfred.name" . }}" --tail=50

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📈 Metrics (if monitoring is enabled):

  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "alfred.fullname" . }} 8000:8000
  
  Visit: http://localhost:8000/metrics

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 Next Steps:

1. Configure your database connection:
   kubectl create secret generic alfred-db-secret \
     --from-literal=database-url='postgresql://user:pass@host:5432/alfred' \
     --namespace {{ .Release.Namespace }}

2. Add LLM provider API keys:
   kubectl create secret generic alfred-llm-secrets \
     --from-literal=openai-api-key='sk-...' \
     --from-literal=anthropic-api-key='sk-ant-...' \
     --namespace {{ .Release.Namespace }}

3. Run database migrations:
   kubectl exec -it -n {{ .Release.Namespace }} \
     $(kubectl get pod -n {{ .Release.Namespace }} -l app.kubernetes.io/name=alfred -o jsonpath='{.items[0].metadata.name}') \
     -- alembic upgrade head

4. Create your first admin user:
   kubectl exec -it -n {{ .Release.Namespace }} \
     $(kubectl get pod -n {{ .Release.Namespace }} -l app.kubernetes.io/name=alfred -o jsonpath='{.items[0].metadata.name}') \
     -- python -m app.cli create-admin

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 Documentation:
   https://github.com/AlfredDev/alfred/docs

🐛 Report Issues:
   https://github.com/AlfredDev/alfred/issues

💬 Community:
   https://github.com/AlfredDev/alfred/discussions

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  IMPORTANT: 
- Change default passwords before production use
- Configure proper backup strategies
- Review security settings in values.yaml
- Set up monitoring alerts

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Thank you for using Alfred! 🚀
