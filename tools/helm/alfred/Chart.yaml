# Alfred Helm Chart
#
# [AI GENERATED - GOVERNANCE PROTOCOL]
# ──────────────────────────────────────────────────────────────
# Model:       Claude Opus 4.6
# Tier:        L2
# Logic:       Helm chart for deploying Alfred to Kubernetes.
# Root Cause:  Sprint task T189 — Helm chart.
# Context:     Production-ready K8s deployment configuration.
# Suitability: L2 — Standard Helm chart patterns.
# ──────────────────────────────────────────────────────────────

apiVersion: v2
name: alfred
description: Alfred AI Credit Governance Platform Helm Chart
type: application
version: 1.0.0
appVersion: "1.0.0"

keywords:
  - ai
  - llm
  - governance
  - cost-management
  - openai
  - anthropic

home: https://alfred.ai
sources:
  - https://github.com/AlfredDev/alfred

maintainers:
  - name: Alfred Team
    email: helm@alfred.ai

dependencies:
  - name: postgresql
    version: "~12.0"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
  - name: redis
    version: "~17.0"
    repository: "https://charts.bitnami.com/bitnami"
    condition: redis.enabled
