"""
# Template for test credentials.
# Copy this file to `tests/fixtures/credentials.py` and populate values locally or in CI secrets manager.
# DO NOT commit real secrets into source control. Store actual secrets in CI environment variables or secret stores.

# Example structure expected by `tests/conftest.py`:
CREDENTIALS = {
    # 'TEST_ADMIN_PASSWORD': 'replace_with_secret_in_ci',
    # 'TEST_API_KEY': 'replace_with_secret_in_ci',
}

# Guidance:
# - CI pipelines should set the required env vars (TEST_ADMIN_PASSWORD, TEST_API_KEY, etc.)
# - For local development, keep a `.env` file out of source control and load with python-dotenv if desired.
"""
